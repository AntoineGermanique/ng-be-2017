import { EmailDirectorDirective } from './email-director.directive';

describe('EmailDirectorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDirectorDirective();
    expect(directive).toBeTruthy();
  });
});
